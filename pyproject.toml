[project]
name = "brand-serp-monitor"
version = "0.1.0"
description = "Branded SERP monitoring and domain classification (StarCasino / NL)"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
live = ["requests>=2.31,<3"]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
